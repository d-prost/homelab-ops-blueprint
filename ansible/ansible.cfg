[defaults]
interpreter_python = auto_silent
retry_files_enabled = False
host_key_checking = True
# Relative config paths resolve from this ansible.cfg. Keep repository roles
# discoverable both in a normal checkout and in the disposable git-archive test.
roles_path = ./roles

[privilege_escalation]
become_ask_pass = False
