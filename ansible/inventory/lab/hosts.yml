---
all:
  children:
    lab:
      hosts:
        ephemeral-lab:
          ansible_connection: local
          ansible_host: 127.0.0.1
          ansible_user: runner
          homelab_environment: lab
          homelab_expected_hostname: "{{ lookup('env', 'HOMELAB_LAB_HOSTNAME') }}"
